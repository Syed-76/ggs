- [Discord bot emoji system](discord-bot-emoji-system.md) — this bot has a central custom-emoji helper (`utils/emojis.py`); help command intentionally keeps custom emojis, all other messages use plain Unicode emojis.
- [Branding global defaults](branding-global-defaults.md) — guild_id=0 in guild_branding table stores global defaults; get_branding() falls back to global then to hardcoded defaults. reset_branding() deletes guild row to revert.
- [Fun commands system](fun-commands-system.md) — Root cause: MESSAGE_CONTENT intent missing in Dev Portal → empty content → silent fail. Fix: /sun slash command (no intent needed) + on_message as bonus.
- [Bot deploys on Render not Replit](bot-deploy-context.md) — discord.py not installed in Replit env; py_compile is the syntax check; never expect `import discord` to succeed in this shell.
- [Prefix locked to > and activity persistence](prefix-activity-decisions.md) — prefix hard-locked in get_prefix; custom activity persisted via bot_activity table in branding.db; use_custom_activity flag on bot stops rotating status task.
- [Slash command prefix alternatives pattern](slash-prefix-alt-pattern.md) — add @commands.command methods in the same Cog class; watch indentation (methods nested inside module-level helpers become local fns, not bot commands).
- [hybrid_group bare-invoke pitfall](hybrid-group-bare-invoke.md) — bot.get_command(name) on a group always returns the parent; bare callback should perform the action, not just send_help, if bare invocation is expected to work.
- [Render data persistence pattern](render-persistence.md) — use DATA_DIR env var + symlink strategy in CodeX.py; migrates db/, data/, root .db files recursively to persistent disk on first boot. Render persistent disk must be mounted at DATA_DIR path.
- [J2C interface channel is static](j2c-interface-design.md) — setup creates read-only #interface channel with branded embed (yellow, banner, no thumbnail, no buttons); VC control panel inside the created VC keeps buttons. create_interface_embed() is async (needs branding fetch). create_vc_control_embed() is also async; all callers must await it.
- [Ticket system design](ticket-system-design.md) — rewritten: setup always shows landing (Create New / Edit Existing); ButtonManagerView edits its own message in-place for TicketEmbedConfigView; persistent TicketActionsView/ClosedTicketView use static custom_ids, look up ticket by inter.channel.id not stored ch_id.
- [Poll ChannelSelect channel resolution](poll-channel-resolution.md) — ChannelSelect.values[0] is AppCommandChannel (no .send()); must store .id and resolve via guild.get_channel() before permission checks and sending.
- [Slash command cap strategy](slash-vs-prefix-command-cap.md) — sync_commands/sync_commands_debug kwargs are no-ops in real discord.py; cap avoided by keeping most commands prefix-only, only curated ones as hybrid_command.
- [Music system custom emojis](music-emoji-pattern.md) — MusicControlView sets custom emojis+labels in __init__ by iterating self.children; button order is fixed (decorator order); _pe() helper in music.py converts emoji name→PartialEmoji. zloop.png and zautoplay.png need /upload emojis branch:music.
- [Playlist system DB pattern](playlist-db-pattern.md) — PRAGMA foreign_keys=ON per-connection in aiosqlite; PlaylistDB._setup() applies WAL+FK pragmas; delete_playlist does explicit child-row delete. DB at db/playlist.db. Emoji IDs now in db/emoji_store.db (no JSON file).
- [Branding string policy](branding-string-policy.md) — Never hardcode "Zyrox", "Zyrox X", or "CodeX Studio" in user-facing content; import DEFAULT_BRANDING from utils.branding and use f-strings. Bot never sends vote/support server links. Dropdown placeholders have no custom emojis (Discord renders them poorly).
- [Music wavelink event names](music-wavelink-events.md) — wavelink 3.x events need @commands.Cog.listener() + correct name: on_wavelink_track_end (not on_track_end), on_wavelink_track_start, on_wavelink_track_exception, on_wavelink_track_stuck. Missing decorator = event never fires.
- [Playlist queue progression](music-playlist-queue.md) — resolve playlist tracks before starting the first one, or a short track can end while the queue is still empty.
- [otakugifs broken endpoints](otakugifs-broken-endpoints.md) — 10 endpoints return 400; nekos.best/waifu.pics also fail; remap broken ones in _OTAKUGIFS to nearest working alternative.
- [Fun prefix configurable](fun-prefix-configurable.md) — fun_prefix column added to guild_branding table; get_fun_prefix/set_fun_prefix in utils/branding.py; guild_id=0 = global default; fun.py on_message reads it dynamically; AppearanceView + GlobalAppearanceView both show a Fun Prefix button.
- [Emoji store reload after Neon restore](emoji-restore-timing.md) — call reload_store() after restore_from_neon() in CodeX.py main(); emojis.py _store is set at module import time so must be refreshed after DB restoration.
- [Supabase integration boundary](supabase-integration-boundary.md) — keep Supabase additive until a deliberate table-by-table migration is requested; Render remains the production source of environment variables.
